{
  "name": "@viaggigoated/sdk",
  "version": "1.0.0",
  "description": "SDK TypeScript per viaggigoated — client tipato su openapi.yaml",
  "type": "module",
  "main": "src/client.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "orval": "^7.0.0"
  }
}
